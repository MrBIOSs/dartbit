name: dartbit
description: (Unofficial) Dart library for interacting with the Bybit API.
version: 0.0.1
repository: https://github.com/MrBIOSs/dartbit

environment:
  sdk: ^3.7.2

dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
